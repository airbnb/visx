import React from 'react';
declare function Page({ children, title, className, wrapper, }: {
    children: React.ReactNode;
    title?: string;
    className?: string | boolean;
    wrapper?: boolean;
}): JSX.Element;
export default Page;
