import React from 'react';
export declare type GlyphProps = {
    /** Top offset to apply to glyph g element container. */
    top?: number;
    /** Left offset to apply to glyph g element container. */
    left?: number;
    /** classname to apply to glyph g element container. */
    className?: string;
    /** Children to render. */
    children?: React.ReactNode;
};
export default function Glyph({ top, left, className, children }: GlyphProps): JSX.Element;
//# sourceMappingURL=Glyph.d.ts.map