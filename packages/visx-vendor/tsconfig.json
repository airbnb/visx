{
  "extends": "../../tsconfig.options.json",
  "compilerOptions": {
    // note: this is needed to compile tests/ which have 
    // unused locals to test their proper export
    "noUnusedLocals": false,
  },
  "references": [],
  "include": [
    "./package.json"
  ]
}