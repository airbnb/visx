import React from 'react';
import { shallow } from 'enzyme';
import { CustomProjection } from '../src';

describe('<CustonProjection />', () => {
  test('it should be defined', () => {
    expect(CustomProjection).toBeDefined();
  });
});
