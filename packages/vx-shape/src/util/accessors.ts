import { $TSFIXME } from '../types';

export const getX = (l: $TSFIXME) => l?.x;

export const getY = (l: $TSFIXME) => l?.y;

export const getSource = (l: $TSFIXME) => l?.source;

export const getTarget = (l: $TSFIXME) => l?.target;
